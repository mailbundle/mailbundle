These are the "sources" for the configuration.
You can set some variables in a special `variables.py`.
Each `.jinja` file will be processed and copied (stripped of its jinja
extension).
Each `.static` files will be copied without any template expansion.
